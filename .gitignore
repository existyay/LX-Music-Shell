# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/

# Build files
*.tar.gz
*.pkg.tar.zst
*.tar.bz2

# Config files (user-specific)
.config/lx-music-shell/config
.config/lx-music-shell/sources.list
.cache/lx-music-shell/

# Test output
*.log

# Security: SSH keys and credentials
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
id_dsa*
id_ecdsa*
authorized_keys
known_hosts
*.gpg
*.asc
.env
.env.*
credentials*
secrets*
*.secret
*token*
*password*

# AUR submission helpers (local-only tools)
# Local AUR working copies and build outputs
aur/lx-music-shell-git/
aur/LX-Music-Shell-*.tar.gz
aur/*.pkg.tar.zst
aur/*.tar.gz
aur/PKGBUILD.bak
aur/pkg/
aur/pkg/**
aur/src/
aur/src/**

# Build artifacts
*.o
*.so

# Python
__pycache__/
*.pyc
*.pyo

# Temporary
*.tmp
*.bak
*.orig

# Design specs and internal docs (not committed to repo)
docs/superpowers/
docs/plans/
docs/legacy/

# 参考项目 (本地克隆, 仅用于研究, 不提交)
references/

# Keep AUR metadata (PKGBUILD/.SRCINFO) and helper scripts tracked
# (remove specific ignores above that would hide needed files like push-to-aur.sh)package-lock.json
node_modules/
package*.json
