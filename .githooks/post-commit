#!/usr/bin/env bash
# post-commit hook: 仅做信息性提示
# 注意: 不主动调用 build-aur-package.sh,避免覆盖已 commit 的 PKGBUILD/版本信息
# 开发者应手动运行 aur/build-aur-package.sh 并 commit 结果

set -uo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

# 只做提示,不修改任何文件
exit 0
